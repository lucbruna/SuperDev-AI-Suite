# Themes

Custom theme files for the application.

## Available Themes

- `light.css` - Light theme (default)
- `dark.css` - Dark theme
- `high-contrast.css` - High contrast accessibility theme

## Creating a Theme

1. Copy an existing theme
2. Modify CSS variables
3. Register in theme selector
