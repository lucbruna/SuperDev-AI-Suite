"use client";

/**
 * Dashboard component - re-exported from the main page.
 *
 * The primary dashboard lives in `frontend/src/app/dashboard/page.tsx`.
 * This file exists for backward compatibility if other modules import
 * `@/dashboard/Dashboard`.
 */

export { default as Dashboard } from "@/app/dashboard/page";
