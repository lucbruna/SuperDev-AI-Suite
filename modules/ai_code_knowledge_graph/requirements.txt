# AI Code Knowledge Graph — runtime dependencies.
# Core scanning pipeline works with the Python standard library only.
#
# Optional (feature-gated at import time):
#   pyyaml            YAML scanning (falls back to line-based scan)
#   fastapi           REST API (module api/)
#   pydantic          API request models
#   numpy             embeddings math
#   redis             distributed cache / vector store
#   psycopg2-binary   postgres graph storage
#   neo4j             graph database storage
#   httpx             llm/ integrations
fastapi>=0.100
pydantic>=2.0
PyYAML>=6.0
