# Changelog

All notable changes to the Digital Twin module are documented here.

## [1.0.0] - 2026-08-05

### Added
- Fase A — scaffolding do módulo (`__init__`, `version`, `requirements`,
  `README`, `LICENSE`, `CHANGELOG`).
- `config/` — `DigitalTwinConfig` (env `SUPERDEV_DT_*`), `SimulationConfig`,
  `PredictionConfig`, `SyncConfig`, `MonitoringConfig`, `MemoryConfig`,
  `Permissions` (papéis viewer/operator/admin), `constants`.

### Planned
- `core/`, `api/`, `twin_engine/`, `project_model/`, `state_manager/`,
  `synchronization/`, `simulation/`, `prediction/`, `analytics/`,
  `metrics/`, `events/`, `agents/`, `workflows/`, `plugins/`, `graph/`,
  `memory/`, `database/`, `monitoring/`, `visualization/`, `reports/`,
  `integrations/`, `websocket/`, `scheduler/`, `frontend/`, `cli/`,
  `utils/`, `docs/`, `tests/`.
