apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: superdev-dev
resources:
  - ../../base
patches:
  - patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
    target:
      kind: Deployment
namePrefix: dev-
labels:
  - pairs:
      environment: dev