apiVersion: v1
kind: Service
metadata:
  name: {{ include "superdev.fullname" . }}
  labels:
    {{- include "superdev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "superdev.selectorLabels" . | nindent 4 }}