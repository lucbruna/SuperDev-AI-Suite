# Changelog

All notable changes to the SuperDev AI Suite will be documented in this file.

## [Unreleased]

### Added
- **AI Avatar & Digital Human Engine (AI Video Studio Volume 6)** — `modules/ai_video_studio/ai_avatar_engine/` complete with **146 files across 10 subsystems**: core (AvatarEngine orchestrator, manager, registry, scheduler, optimizer, learning, statistics, cache, logger, profiles, permissions, metadata, export/import), digital_humans (15 procedural generators + proportions/variations/ages), facial_animation (rig, 23-landmark mesh, solver + 10 feature controllers), emotions (12 presets + blending with easing), gestures (engine + 15 context vocabularies), motion_capture (keypoints → pose → skeleton → retarget → JSON export), clothing (8 garment generators + wardrobe/materials/textures/palettes), hairstyles (9 catalogs + color engine), library (**48 virtual actors in 15 domains**), training (5 learning systems + RL bandit + quality validation + model versioning)
- **Avatar Engine REST API:** 9 endpoints under `/api/v1/video-studio/avatar-engine/*` (profiles GET/POST, generate, emotions, gestures, clothing, hairstyles, motion-capture, stats) wired into `modules/ai_video_studio/api/router.py`; Swagger UI at `/api/v1/video-studio/docs`
- **Volume 6 documentation:** `modules/ai_video_studio/ai_avatar_engine/README.md` (architecture, subsystems, quick start, API reference, integrations); SESSION_STATE entry registered
- **Tests:** `tests/unit/test_avatar_engine_v6.py` (33 tests) — generation pipeline, library, emotions, gestures, clothing, hairstyles, mocap, training, API routes
- **Global Suite Integration (AI Video Studio Volume 10, Part 1 — SuiteBridge):** `modules/ai_video_studio/suite_integration/` (12 files) — `SuiteBridge` + `SuiteManifest` + 6 adapters that reuse the real platform (integration engine via `IntegrationDefinition`, `backend.auth.jwt`, `SuperDev.security.ssrf` with stdlib fallback, `SuperDev.monitoring`, `SuperDev.workflow`; `SuperDev.plugin_platform` currently broken in the repo so the plugin adapter uses a local descriptor registry with 8 official studio plugins); REST routes `/api/v1/video-studio/suite-integration/*`; `suite_bridge` registered as an integration-manager service; `tests/unit/test_suite_integration.py` (16 hermetic tests)
- **Global Suite Integration (Volume 10, Part 2 — 17 domain connectors):** `modules/ai_video_studio/integration/` — shared base (`connector_base.DomainConnector` contract status/capabilities/execute, `_brief.VideoBrief`, `connectors_registry` lazy + failure-tolerant) + 17 business-domain connectors: enterprise_ai (LLM router, prompt dispatcher, multi-agent, reasoning, memory, knowledge, embeddings, vector DB), agriculture_ai, erp, crm, human_resources, finance, business_intelligence, security (permission/audit/encryption/auth bridges), automation, notifications (local outbox), knowledge (RAG), cloud (5 providers, dry-run), monitoring, supervisor, gateway (REST/WS/gRPC/events), message_bus (kafka/rabbitmq/redis/nats/mqtt), learning (RL bandit + quality feedback); auto-registered in the integration manager + `GET /api/v1/video-studio/integration/connectors`; `tests/unit/test_connectors_v10.py` (21 tests); docs in `integration/README.md`; SESSION_STATE entry registered

### Fixed
- **wardrobe_manager color selection:** operator-precedence bug made the accent color equal the primary color; now guaranteed to differ
- **facial_engine imports:** per-feature controllers now lazy-imported (avoided a top-level import cycle); `emotional_blending` lazy import of `emotion_engine` resolves the circular import
- **avatar_registry:** removed dead `_logger` assignment; `avatar_manager` no longer reaches into registry internals
- **Stale version assertions in tests:** `tests/unit/test_api_docs.py`, `tests/unit/test_metrics_endpoint.py`, `tests/unit/test_schemas_validation.py`, `tests/integration/test_api_e2e.py` updated from the old `5.0.0` expectations to `6.0.0` (suite went from 3 failures to green)

## [6.0.0] - 2026-08-02

### Changed
- **Single-source versioning:** `VERSION` at the repo root is now the single source of truth for the suite version. `scripts/sync_version.py` propagates it to `pyproject.toml` and `backend/pyproject.toml` (`[project] version`), `package.json` and `frontend/package.json` (top-level `"version"`) and the runtime mirror `backend/constants.py` (`VERSION: Final[str]`). `--check` mode fails on any drift and runs in CI (`python scripts/sync_version.py --check`) plus the `make version-check` target. Independently versioned packages (sdk/, templates/, builders/, admin-dashboard/, desktop/, extensions/) are intentionally untouched. Bump flow: `echo "X.Y.Z" > VERSION && make version`.
- **Automatic release pipeline:** new `.github/workflows/release.yml` — manual `workflow_dispatch` with `version` input (e.g. `v6.1.0`) and `dry_run` flag; validates the format, refuses existing tags, guards drift with `make version-check`, bumps via `echo "..." > VERSION && make version && make version-check`, commits the bumped metadata, tags and creates a GitHub Release (softprops/action-gh-release). `.github/workflows/cd.yml` gained a fail-fast `make version-check` guard before AWS config so deploys never ship with drifted metadata.
- **Modern lifespan startup:** removed deprecated `router.on_event("startup")` hooks from `backend/code_search/api.py` and `backend/cloud/api.py`; initialization moved to the central lifespan startup (`backend/startup.py`).
- **Tests run with OTEL disabled** (`OTEL_ENABLED=false`) — removes OTLP export noise; backend suite dropped from ~29s to ~11s.

### Fixed
- **JWT/secret hardening (audit C1/C3):** `backend/settings.py` insecure `secret_key` default removed (now `""`) with a validator rejecting known placeholder keys; shared `validate_secret_key()` guard in `backend/auth/jwt.py` used by both `JWTManager` and `AuthManager` (no code path signs tokens with a guessable key); local `.env` regenerated with strong 64-hex `SECRET_KEY`/`JWT_SECRET_KEY`; `backend/tests/conftest.py` + `tests/conftest.py` force a dedicated test key (deterministic suite, independent of `.env`/shell).
- **Hardcoded credentials removed:** `backend/main_simple.py` generates a random admin password per boot when `ADMIN_PASSWORD` is absent; `backend/scripts/seed_database.py` no longer uses `admin123` (env-driven or random); `backend/cli/main.py` scaffolds generate a random `JWT_SECRET_KEY` instead of a placeholder.
- **Lint (ruff):** `datetime.UTC` ×3, `TimeoutError`, `N806` — 0 errors across `backend/`.
- **Pydantic deprecation:** `class Config` → `ConfigDict` in `backend/api/v1/knowledge.py` (2 warnings removed).
- **Duplicate lockfile:** root `package-lock.json` removed (repo uses pnpm); `frontend/package-lock.json` preserved (frontend uses npm).
- **Fixture bug:** `settings.SECRET_KEY` → `settings.auth.secret_key` in test conftests.

## [5.1.0] - 2026-07-31

### Added
- **Code navigation pipeline (AST + DependencyGraph):** `code/parsing/ast_manager.py` — `ASTManager.parse()` now uses `ast.parse` to extract imports (`Import`/`ImportFrom` with module/names/asname/level, incl. the `from . import X` module=None fallback), module-level classes and functions (incl. async), returning a structured dict or `None` on syntax error, plus `to_dict()` AST serialization; `code/understanding/dependency_graph.py` — `DependencyGraph` with deduplicated `add`, `get_dependencies`, `get_dependents` (reverse edges), `nodes`, `edges`, `to_dict` and `build(files)` that parses `CodeFile` objects or dicts via `ASTManager` and adds `path -> imported-module` edges automatically, reporting syntax errors in the summary; wired into `ParserEngine.parse` (python delegation) and `DependencyAnalysis.analyze` (graph building); exports updated in `code/parsing/__init__.py` and `code/understanding/__init__.py`; tests in `code/tests/test_ast_manager.py` + `test_dependency_graph.py`
- **LLM code navigation (SymbolIndex + ContextBuilder + PromptBuilder):** `code/understanding/symbol_index.py` — `SymbolIndex` populated from `ASTManager.parse` results (`index_parsed` for classes/functions/imports, `index_file` skipping syntax-error files, `index_files` accepting `CodeFile` or dicts, case-insensitive `search`, `files`/`count`/`to_dict`, keeping the original `add`/`find` API); `code/understanding/context_builder.py` — `ContextBuilder` runs **BFS on the dependency graph** from seed files walking both dependencies and dependents, bounded by `max_depth`/`max_files`/`max_tokens` (seeds always kept), with a `len//4` token heuristic and depth metadata; `code/understanding/prompt_builder.py` — `PromptBuilder` composes instruction + `### FILE: path` fenced blocks, `build_from_selection` injects a `ContextBuilder` selection, `tokens`/`fits_budget`; `CodeUnderstanding.understand(path)` now scans → indexes symbols → builds the graph end-to-end; exports updated in `code/understanding/__init__.py`; tests in `code/tests/test_symbol_index.py` + `test_context_builder.py` + `test_prompt_builder.py`
- **CodeEngine LLM-navigation wiring + executable example:** `code/code_engine.py` — `CodeEngine` now exposes `scanner`/`factory` (lazy import, avoids the circular `code_factory` import) and three async navigation methods: `understand(path)` (delegates to `CodeUnderstanding`), `find_symbols(path, query)` (index + search with kind/path locations), and `build_llm_context(path, seed_files, instruction, max_depth/max_files/max_tokens)` which scans, indexes, builds the module graph, **resolves module-name edges to real file paths** via `_module_to_path_map` (`services/order_service.py` → `services.order_service`, `__init__.py` → package name) so the BFS walks actual files in both directions (skipping stdlib/3rd-party targets), and composes the final prompt via `PromptBuilder` with token estimates; also fixed pre-existing `test_code_engine.py` failures (`CodeIssueSeverity.CRITICAL` added, `engine.factory`/`engine.scanner`); new executable example `examples/llm-navigation/` — `main.py` (understand → find_symbols → BFS context → prompt) over a real multi-package `demo_project/` with imports (main → order_service/helpers, order_service → order/helpers, order → base, **sales_report → order_service reverse edge**), plus `README.md`; tests in `code/tests/test_example_llm_navigation.py`
- **CodeEngine → LLMEngine integration (`ask_llm`):** `code/code_engine.py` — `CodeEngine` now connects the navigation pipeline to the real `ai/llm` layer via a lazy `llm` property (imports `ai.llm.LLMEngine` on first access, cached in `self._llm`, with `TYPE_CHECKING` annotations) and `ask_llm(path, seed_files, instruction, provider, model, max_tokens, temperature, llm=None, **kwargs)`: builds the anchored prompt through `build_llm_context`, auto-registers providers from env vars when none were passed, falls back to a `MockProvider` when no provider is available **or the routed provider returns an empty reply** (missing/expired key, no network, auth failure — graceful degradation with `mode: mock`), executes via `LLMEngine.execute` and returns `{prompt, prompt_tokens, anchored_files, response{content, provider, model, tokens_prompt, tokens_completion, latency_ms, cost_usd, finish_reason}, mode}`; example step 5 + README updated; tests in `code/tests/test_example_llm_navigation.py` (`TestCodeEngineAskLLM`: deterministic mock fallback via monkeypatched `auto_register_providers`, injected-engine usage, explicit-provider passthrough)
- **Relative-import resolution in `build_llm_context`:** `code/code_engine.py` — new `_resolve_import_to_path(rel, imp, module_map)` resolves every parsed import to a real file path: absolute imports try the submodule first (`from services import order_service` → `services/order_service.py`), and **relative imports** (`from . import X`, `from ..pkg import Y`) use the AST `level` plus the importing file's package depth (`base = dirs minus level-1`), falling back to the package `__init__.py` when `from . import X` defines X there; `build_llm_context` now parses each file directly with `ASTManager` (the previous `DependencyGraph` edges dropped the `level` info) so the BFS walks files reached via relative imports and `__init__.py` sub-modules; demo project updated with real relative imports (`models/order.py` `from .base`, `services/__init__.py` `from . import helpers`, new `services/helpers.py` `from ..utils.helpers`, `services/order_service.py` `from ..models.order`/`..utils.helpers`/`.helpers`, `reporting/sales_report.py` `from ..services.order_service` reverse edge); new tests `code/tests/test_module_resolution.py` (8 unit tests) + `test_relative_imports_resolve_to_real_files` in `test_example_llm_navigation.py` (cross-platform `_has_suffix` path matching)
- **Relevance ranking in `build_llm_context`:** `code/code_engine.py` — new `_rank_selection(selection, index, query)` scores each selected file by the symbols matching the query (case-insensitive `SymbolIndex.search`, weighted by kind: class=3 > function=2 > import=1); seed files (depth 0) keep their position and the rest sort by descending relevance (stable, ties by depth); every selection entry gains `relevance` and `matched_symbols`, and the ranked order feeds `PromptBuilder` so the most relevant files are injected first. `build_llm_context` gained a `query` param (None keeps the original BFS order, entries get relevance 0) and returns it in the result; `ask_llm` forwards `query`. Example `main.py` step 3 passes `query='Order'` and prints the ranked selection with scores and matched symbols; `README` documents the new step. Tests: `TestRankSelection` (5 unit tests) in `test_module_resolution.py` + `test_query_ranking_prioritizes_relevant_files` in `test_example_llm_navigation.py`
- **Public relevance-ranking API on `SymbolIndex`:** `code/understanding/symbol_index.py` — new `rank(query)` returns matching symbols as `[{name, locations, relevance}]` sorted by **descending relevance** (each symbol's score is the sum of per-location kind weights from the new module-level `RELEVANCE_WEIGHTS = {class: 3, function: 2, import: 1}`, so a class defined in two files outranks a single function; stable for ties; blank query matches everything; `RELEVANCE_WEIGHTS` re-exported from `code/understanding/__init__.py`). Reused in `code/code_engine.py`: `_rank_selection` now consumes `index.rank(query)` (same order-independent per-file accumulation — results identical) and `find_symbols` returns `index.rank(query)` directly, so its matches come back already sorted by relevance (each match gains `relevance`); docstring updated. Tests: `TestSymbolRank` (6 unit tests) in `test_symbol_index.py` + `test_find_symbols_matches_sorted_by_relevance` in `test_example_llm_navigation.py` asserting the actual contract (relevance non-increasing, top match is a class, trailing matches are plain imports)
- **Per-file token truncation in `PromptBuilder`:** `code/understanding/prompt_builder.py` — new `max_file_tokens: int | None` param (None = disabled, backward compatible); `_truncate_content(path, content)` truncates oversized files **in the middle of their `### FILE` block**: keeps the head lines fitting `budget//2` tokens and the tail lines fitting the rest, inserting a marker line `# ... [N lines / ~M tokens truncated] ...`, with a character-slicing fallback when a single line exceeds the budget (matches the ~4 chars/token heuristic); truncated paths are tracked in `last_truncated` (reset per `build`). Wired in `code/code_engine.py`: `build_llm_context` gained `max_file_tokens` (forwarded to `PromptBuilder`, result adds `truncated_files` count and `max_file_tokens`) and `ask_llm` forwards it — so the **ranked selection survives tight budgets**. Example `main.py` step 5 demonstrates a tight-budget run (`max_tokens=600`, `max_file_tokens=30` → 5 files truncated, prompt still fits); README updated. Tests: `TestPromptTruncation` (6 unit tests) in `test_prompt_builder.py` + `test_tight_budget_truncates_files_in_middle` in `test_example_llm_navigation.py`
- **Global token budget in `PromptBuilder` (truncate/drop until the whole prompt fits):** `code/understanding/prompt_builder.py` — `build` now enforces `max_tokens` on the **entire assembled prompt**, independently of the per-file cap: while the prompt exceeds `max_tokens`, the trailing file (least relevant, since selections are ranked) is re-truncated in the middle down to the remaining budget and, when even its minimal slice cannot fit, dropped entirely (tracked in `last_dropped`). The line-based split reserves the marker cost (`_MARKER_RESERVE = 12`) and its post-assembly verify uses a 2-token margin (`_SLICE_VERIFY_MARGIN`) because `estimate_tokens` floors each part — a slice fitting *budget* by its own estimate can still overshoot the enclosing `### FILE` block by 1-2 tokens; boundary slices fall back to the guaranteed-fit `_char_slice` (reserves marker + 4 tokens, rstrips the head and reallocates to the tail when it collapses to whitespace, e.g. keeps `zzzz` of a `"\n\n" + "z" * 2000` single-line file). An `shrunk != content` guard prevents an infinite loop in the global pass (when a slice already fits, re-running truncation returns it unchanged). Wired in `code/code_engine.py`: `build_llm_context` returns `dropped_files` (count) and documents the global pass. Example `main.py` section 5b demonstrates `max_tokens=150` **without** `max_file_tokens` (1 file truncated, 0 dropped, prompt still fits); README updated. Tests: `TestPromptGlobalBudget` (6 unit tests) in `test_prompt_builder.py` + `test_global_budget_without_max_file_tokens` and main-entrypoint asserts in `test_example_llm_navigation.py`
- **Focus section in `ask_llm` from the relevance ranking:** `code/code_engine.py` — `build_llm_context` now also returns `ranked_symbols` (the query's `SymbolIndex.rank` matches, so callers reuse the ranking without re-scanning); new `_focus_symbols(ranked, limit=4)` keeps the most relevant **defined** symbols in relevance order (pure imports skipped — module names are noise in a focus line). `ask_llm` gained `focus: bool = True` and `focus_limit: int = 4`: when `query` is given, the top focus symbols are appended to the instruction as a `Foco: sym1, sym2` section (the context is re-built so the section lands in the prompt) and the result carries `focus = {symbols, section, overhead_tokens, coverage}` — `coverage` measures the fraction of focus symbols actually mentioned in the reply (substring match; a cheap deterministic proxy for response improvement, compare against a `focus=False` baseline). Example `main.py` step 6 sends `query='Order'` and prints the focus section/coverage; step 7 registers a deterministic `FocusEchoProvider` (MockProvider that echoes the `Foco:` line) and shows baseline 0% → focused 100% coverage with +13 token overhead. Tests: `TestFocusSymbols` (4 unit tests) in `test_module_resolution.py` + focus tests in `test_example_llm_navigation.py` (section added, focus disabled, full coverage with echo provider, main-entrypoint measurement assertions)
- **Security Engine (Volume 16)** — `security/` module completed: `SecurityEngine` orchestrator coordinating 15 subsystems (existing owasp/sbom/secrets_detector/vulnerability_engine/dependency_scan + encryption, hashing, signatures, certificates, vault, secrets, integrity, compliance, security_scan, threat_detection) with aggregate `run_scan` and composite `security_score`
- Security core infrastructure: `security_config`, `security_models` (EncryptedPayload, HashResult, SignatureResult, CertificateInfo, VaultSecret, IntegrityReport, ComplianceResult, SecurityScanResult, ThreatEvent + StrEnum enums), `security_events`, `security_metrics`, `security_logger`, `security_security` (RBAC SecurityGuard with audit trail), `security_context`, `security_runtime`, `security_registry`, `security_factory`, `security_manager`, `security_interfaces`, `security_protocols`
- New functional subsystem engines (stdlib-only): symmetric encryption with roundtrip, hashing (sha256/sha512/blake2b + PBKDF2 salted passwords + HMAC), HMAC signatures, certificate lifecycle (issue/validate/rotate/expiry), secret vault (TTL, versions, rotation), secret generation/strength/entropy validation, integrity checksums with baseline/tamper detection, compliance evaluation (SOC2/GDPR/HIPAA with score and gaps), aggregated security scanning with weighted risk score, threat detection heuristics (brute-force, exfiltration, unauthorized access) with mitigation
- Integration: `ai_platform/__init__.py` re-exports `security.*` via safe imports (20 exports); real example `examples/security-engine/main.py` running encryption → hashing → signatures → vault → integrity → compliance → threat detection → aggregated scan
- Passing tests covering the engine, lifecycle, subsystem wiring and crypto/ops subsystems
- **Testing & Quality Engine (Volume 15)** — full `quality/` module: `QualityEngine` orchestrator coordinating 12 subsystems (testing, unit, integration, regression, performance, security, automation, coverage, analysis, benchmarking, reports, validation) with `compute_quality_score` (code/tests/security/performance/documentation dimensions) and `validate_production` production gate
- Quality core infrastructure: `quality_config`, `quality_models` (TestSuite/TestCase/TestResult + status enums), `quality_events`, `quality_metrics`, `quality_logger`, `quality_security`, `quality_context`, `quality_runtime`, `quality_registry`, `quality_factory`, `quality_manager`, `quality_interfaces`, `quality_protocols`
- Functional subsystem engines (stdlib-only): testing suites + runner, unit generator/executor/assertions, integration (API/database/service/workflow), regression (baseline + change detector), performance (load/stress/endurance + latency/throughput), security (vulnerability + dependency scan), automation (test generation + parallel runner + retry), coverage (line/function + quality score), analysis (complexity/maintainability/duplication/architecture), benchmarking (suite + comparison + ranking), reports (test/quality/security/performance/executive + export), validation (rules/policies/approval/compliance)
- Integration: `ai_platform/__init__.py` re-exports `quality.*` via safe imports; real example `examples/testing-quality/main.py` running unit tests → coverage → analysis → security scan → quality score → production gate → JSON report
- **Quality Gate <-> DevOps integration:** `devops/deployment/quality_gate.py` (`DevOpsQualityGate` with lazy `QualityEngine` loading and a safe `unavailable` fallback) wired into `DevOpsEngine.deploy_with_quality` — the production gate blocks deploys below quality standards (score/coverage/tests/security signals) and records `devops.deploys` / `devops.deploys_blocked` counters; example `examples/devops-quality-gate/main.py` (weak deploy BLOCKED → good deploy APPROVED); tests in `devops/tests/test_quality_gate.py`
- **Real DeploymentEngine (deploy/rollback/status):** `devops/deployment/deployment_engine.py` fully implemented — orchestrator with pluggable strategies (rolling/canary/blue_green registered by default), in-memory state machine (deploying → healthy/failed → rolled_back/cancelled), `deploy`/`rollback`/`status`/`list`/`cancel`/`history`/`advance` (canary traffic steps)/`switch` (blue-green), `DeploymentSpec` validation, `DeploymentHistory` audit trail (diff + JSON export), `DeploymentHealth` monitoring with auto-rollback, lifecycle events and metrics (`devops.deploys`, `devops.deploys_failed`, `devops.rollbacks`, `devops.deploys_cancelled`); strategies `RollingDeployment` (batches), `CanaryDeployment` (10→25→50→100% traffic), `BlueGreenDeployment` (prepared → switch) implemented
- **deploy_with_quality executes a real deploy:** on gate approval `DevOpsEngine.deploy_with_quality` now runs the actual `DeploymentEngine` (returns `deployment_id` + full deployment record) instead of a simulated result; `DevOpsEngine.deploy`/`rollback`/`status` and `DevOpsManager.deploy_service` delegate to the real engine; example updated to run a real canary deploy → advance traffic → status → rollback → history
- **DevOpsEngine build/provision/destroy/status implemented** (previously `NotImplementedError`): `build` (artifact + service registered in the registry, `devops.builds` metric + `devops.build.completed` event), `provision` (per-environment compute/storage/network/database resources, `devops.provisions` metric), `destroy` (removes the environment and its resources via `DevOpsRegistry.unregister_resource`, `devops.destroys` metric), aggregated `status` (deployments + builds + environments + services, filterable by environment) — following the QualityEngine pattern (own state + metrics + events)
- **DevOpsManager stubs implemented:** `create_environment` (delegates to provision), `get_status`, `list_environments`, `list_services`; **DevOpsFactory** `create_service`/`create_resource` are now real (register `DevOpsService`/resource dicts in the registry)
- **JSON disk persistence:** `DevOpsEngine(store_path=...)` and `DeploymentEngine(store_path=...)` now persist builds (`builds.json`), environments (`environments.json`), deployments (`deployments.json`) and the audit trail (`history.json`) automatically with atomic writes (temp + rename) via `devops/devops_store.py` (`load_json`/`save_json` tolerant to corrupt files); state is restored on `__init__` and explicit `save_state()`/`reload_state()` methods are provided
- **Subsystem engine persistence:** the new docker/environments/terraform/cicd engines now accept `store_path` and persist their own state automatically — `docker.json` (images + builds + containers + logs), `environments_lifecycle.json` (kept separate from the engine's `environments.json` to avoid clobbering), `terraform.json` (per-directory resource state) and `cicd.json` (serialized pipeline definitions + runs); `DevOpsEngine.save_state()`/`reload_state()` now delegate to all four subsystems
- **Subsystem engines implemented (previously stubs):** `docker/` (DockerEngine build/run/stop + ImageBuilder + ImageManager + ContainerManager), `cloud/` (CloudEngine provision/destroy/estimate_cost + ProviderManager + ResourceManager), `environments/` (EnvironmentsEngine create/destroy/activate/promote/variables), `terraform/` (TerraformEngine init/plan/apply/destroy/state), `cicd/` (CICDEngine + PipelineBuilder + PipelineRunner + build/test/security/deploy/approval/artifact stages); `cloud/interfaces.py` added re-exporting `IDevOpsProvider` (fixes the broken import in the existing provider classes)
- **DevOpsEngine delegation:** `build()` delegates to DockerEngine (image) + optional CICD pipeline (`pipeline=...`); `provision()` delegates to CloudEngine (multi-provider resources) + EnvironmentsEngine (lifecycle); `destroy()` delegates to cloud/environments/terraform; lazy props `engine.docker`/`engine.cloud`/`engine.environments`/`engine.terraform`/`engine.cicd`
- Passing tests covering the engine, quality score, production gate and all 12 subsystems
- **Data & Analytics Engine (Volume 12)** — full `data/` module: `DataEngine` orchestrator coordinating 16 subsystems (ingestion, processing, pipelines, warehouse, lake, etl, analytics, bi, machine_learning, forecasting, reporting, visualization, governance, quality, catalog, streaming)
- Core infrastructure: `data_config`, `data_models` (20+ enums/dataclasses), `data_events`, `data_metrics`, `data_logger`, `data_security` (PII masking, classification, audit), `data_context`, `data_runtime`, `data_registry`, `data_factory`, `data_manager`, `data_interfaces`, `data_protocols`
- Ingestion subsystem with pluggable connectors/collectors: `APIConnector` (urllib + pagination with `max_pages` guard), `DatabaseConnector` (sqlite3), `FileConnector` (CSV/JSON/JSONL), `EventCollector`, `LogCollector`, `AgentCollector`, `ProjectCollector`
- Functional subsystem engines: star-schema warehouse, multi-zone data lake with promotion, DAG pipelines, ETL jobs, analytics (descriptive/correlation/segmentation/patterns), BI KPIs and dashboards, ML training/deployment/registry, forecasting with anomaly detection, multi-format reporting, chart visualization, governance policies, data quality profiling, searchable catalog with lineage, real-time streaming with windowing
- Integration: `ai_platform/__init__.py` re-exports `data.*` via safe imports; real example `examples/data-analytics/main.py` collecting agent and project metrics → analytics → executive report
- 88 passing tests covering the engine, models and all subsystems

### Security
- **Safe dynamic-execution guard (`core/safe_exec.py`, OWASP A03 / CWE-94):** new shared AST-allowlist guard for the suite's remaining `exec` points — `guard_code_exec` parses first and blocks imports, calls to non-allowlisted callables (local defs/classes/assignments allowed via `_collect_local_names`), underscore/dunder attribute access and hard-blocked builtins (`__import__`, `eval`, `exec`, `compile`, `open`, `getattr`, `globals`, `vars`, `dir`, `super`); `safe_exec` runs code with restricted `__builtins__` and executes in-place into the caller's namespace (like `exec`); `validate_import_statement` allowlists safe stdlib modules; wired into `core/workflow_engine/nodes/python_node.py` (no more `__builtins__` exposure + import allowlist) and `ai/ai_models/evaluation/coding_score.py` (lazy import so the standalone ai_models smoke script keeps working); regression tests in `tests/unit/test_safe_exec.py`
- **OWASP Top 10 audit of AI-generated code** — critical vulnerabilities found and fixed in existing modules:
- `enterprise_ai_core/workflow_engine.py` — replaced sandbox-escapeable `eval(condition, {'__builtins__': {}})` (RCE path via `().__class__.__mro__...`) with an AST-allowlist condition evaluator (blocks calls/attributes/imports/dangerous names; supports constant collection literals like `role in ['admin', 'superuser']`; fail-safe returns False on any error)
- `ai/debugger/inspector.py` — replaced `eval()` in `evaluate_watch` with a read-only AST evaluator (non-underscore attribute paths + constant-index subscripts allowed; dunders/calls/imports blocked)
- `database/drivers/clickhouse.py` — credentials moved from URL query string to HTTP Basic Authorization header (base64) so passwords never leak into URLs/logs/proxies
- `backend/main_simple.py` — hardcoded admin token replaced with `secrets.token_urlsafe(32)` per boot; plaintext password comparison replaced with `hmac.compare_digest` (constant-time); imports moved to module top
- `docker-compose.yml` + `infrastructure/compose/docker-compose.monitoring.yml` — hardcoded `GF_SECURITY_ADMIN_PASSWORD=admin` replaced with `${GRAFANA_ADMIN_PASSWORD:-changeme}` env var
- `ai/security/encryption/hashing.py` — `quick_hash` documented as non-cryptographic MD5 (performance hashing only, never for passwords/signatures)
- Security regression tests added: `enterprise_ai_core/tests/test_workflow_condition_security.py`, `ai/debugger/tests/test_inspector_security.py`, `database/tests/test_clickhouse_security.py` (26 tests — legitimate conditions/watch expressions work, sandbox-escape injections blocked, ClickHouse URL stays credential-free); SecurityEngine scan now reports 0 findings on the fixed workflow file
- **Full-tree OWASP scan (SecurityEngine.run_scan over the whole SuperDev/ tree) + fixes:**
  - Fixed a real bug that made full-tree scans hang: `secrets_detector` whitelist patterns (with trailing `/`) were matched against directory basenames, so `node_modules`/`.git`/`.venv` were never skipped during `os.walk` — added `SKIP_DIR_NAMES` basename filtering and Windows-safe forward-slash path normalization; full scan dropped from 10+ min hang → ~60s
  - **SQL injection (CRITICAL):** `database/search/fulltext_search.py` — `table`/`columns` identifiers were interpolated into SQL unvalidated; added a strict `^[A-Za-z_][A-Za-z0-9_]*$` allowlist (`_validate_identifier`), bounded LIMIT to 1000, and empty-column short-circuit
  - **Brute-force protection:** new `backend/middleware/auth_rate_limit.py` (`AuthRateLimiter`, per-IP sliding window) wired as a FastAPI dependency on `/login` and `/register`
  - **Weak hashing (MD5→SHA256):** `ai/agents/communication/encryption.py` (obfuscate), `ai/ai_models/caching/response_cache.py`, `ai/ai_models/memory/embedding_manager.py`, `ai/ai_models/security/data_protection.py`, `ai/llm/embeddings/embedding_provider.py`
  - **Insecure generated code:** `builders/backend/builder.py` + `builders/microservices/builder.py` — generated FastAPI/Django/Flask templates now read `CORS_ORIGINS` (no more `allow_origins=["*"]`, whitespace-stripped) and `DEBUG`/`SECRET_KEY`/`ALLOWED_HOSTS` from env; Flask template now imports `os` (fixes a NameError in generated code)
  - **Hardcoded credentials:** `docker-compose.yml` — `POSTGRES_PASSWORD`/`SECRET_KEY` parameterized with env defaults; `.env.example`/`templates/fastapi/.env.example`/`backend/projects/generator.py` — `DEBUG` defaults flipped to `false`
  - Regression tests: `security/tests/test_secrets_detector_skip.py`, `database/tests/test_fulltext_search_security.py`, `backend/tests/unit/test_auth_rate_limit.py` (79 tests green in the touched areas, LINT_CLEAN)
  - Scan delta: 489 → 477 findings; real findings eliminated (OWASP-A02-001 MD5 15→9, OWASP-A05-001 debug 6→1); remaining findings are documented scanner false positives (e.g. OWASP-A01-001=295 is a regex heuristic for missing auth dependency, A02-002=44 HTTP URLs mostly in tests/templates)
- **OWASP audit round 2 — SSRF, shell injection, path traversal:**
  - **Shared SSRF guard (CWE-918):** new `security/ssrf.py` — `validate_public_url(url, allow_private=False)` + `is_internal_host(host)` block private/loopback/link-local/reserved networks (RFC 1918, cloud metadata `169.254.169.254`, IPv4-mapped IPv6) via `ipaddress` + `getaddrinfo`; exported from `security/__init__.py`; DNS-rebinding mitigated best-effort (any internal record blocks), fail-open documented
  - **SSRF in API connectors:** `data/ingestion/api_ingestion.py` `APIConnector.read()` validates once before the pagination loop (was fetching arbitrary URLs incl. metadata endpoints); `data_intelligence/ingestion/api_source.py` `ApiSource._default_request` validates `endpoint` before `urlopen`; both with explicit `allow_private_urls` / `allow_private` opt-in config
  - **SSRF + TLS in AI HTTP tool:** `ai/tools/http_tool.py` — removed `verify=False` (TLS certificate verification was disabled) and added the SSRF guard (returns an error dict instead of raising); protects `research_agent` calls
  - **Shell injection (CWE-78):** `agent_orchestration/executor/command_runner.py` — `subprocess.run(..., shell=True)` was the default; now defaults to `shell=False` with `shlex.split` (argv list) so shell metacharacters (`&&`, `;`) are never interpreted; `shell=True` remains available as an explicit opt-in for trusted commands
  - **Path traversal — sandbox escape (CWE-22):** `core/runtime/filesystem/filesystem.py` `RuntimeFilesystem._resolve_path` now resolves and enforces containment via `is_relative_to(session_root)` — `../` escapes in `read_file`/`write_file`/`delete_file`/`list_files` raise `ValueError`
  - **Path traversal — file connectors:** `data/ingestion/file_ingestion.py` `FileConnector` and `data_intelligence/ingestion/file_source.py` `FileSource` gained an optional `base_dir` containment guard (`_safe_path`/`_contain`) — paths that escape the configured root are rejected
  - **pickle.loads audit: 0 matches** in non-test code — no unsafe deserialization found
  - Regression tests (92 green in touched areas, LINT_CLEAN): new `security/tests/test_ssrf.py`, `tests/unit/test_runtime_filesystem.py`, `ai/tools/tests/test_http_tool.py` + SSRF/path-traversal tests added to the ingestion suites + updated `CommandRunner` tests (cross-platform, injection regression)

## [5.0.0] - 2025-01-15

### Added
- Initial Enterprise release of SuperDev AI Suite
- AI-powered code generation with multi-LLM support (OpenAI, Anthropic, Ollama)
- Multi-agent orchestration system with tool registry and planner-executor pattern
- Visual workflow builder with DAG-based pipeline editor
- Real-time collaborative editing with WebSocket synchronization
- Plugin architecture with hot-reload capability
- Role-based access control with audit logging
- OpenTelemetry integration for distributed tracing
- Prometheus metrics collection and Grafana dashboards
- PostgreSQL storage with asyncpg driver and Alembic migrations
- Redis caching layer with connection pooling
- Object storage abstraction (S3, MinIO, local filesystem)
- Containerized deployment with Docker Compose and Kubernetes manifests
- CLI tool with interactive mode and shell completion
- WebSocket-based terminal emulation via Xterm.js
- Monaco Editor integration with language server protocol support
- Comprehensive test suite with pytest and Playwright
- CI/CD pipeline templates for GitHub Actions and GitLab CI
- API versioning with backward compatibility guarantees
- Structured logging with JSON output and log correlation
- Health check endpoints with readiness and liveness probes
- Rate limiting and request throttling middleware
- CORS configuration with dynamic origin validation
- Secret management with vault backend support
- Database migration automation with alembic
- Multi-stage Docker builds for production optimization
- Health monitoring dashboard with real-time metrics
